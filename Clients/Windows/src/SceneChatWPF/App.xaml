<Application x:Class="SceneChatWPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="UI/MainWindow.xaml">
    <Application.Resources>
        <!-- Colours -->
        <Color x:Key="ColBg">#0A0A0A</Color>
        <Color x:Key="ColSurface">#111111</Color>
        <Color x:Key="ColBorder">#222222</Color>
        <Color x:Key="ColAccent">#39FF14</Color>
        <Color x:Key="ColPurple">#8B5CF6</Color>
        <Color x:Key="ColText">#E0E0E0</Color>
        <Color x:Key="ColMuted">#555555</Color>
        <Color x:Key="ColRed">#DC2626</Color>

        <SolidColorBrush x:Key="BrBg"      Color="{StaticResource ColBg}"/>
        <SolidColorBrush x:Key="BrSurface" Color="{StaticResource ColSurface}"/>
        <SolidColorBrush x:Key="BrBorder"  Color="{StaticResource ColBorder}"/>
        <SolidColorBrush x:Key="BrAccent"  Color="{StaticResource ColAccent}"/>
        <SolidColorBrush x:Key="BrPurple"  Color="{StaticResource ColPurple}"/>
        <SolidColorBrush x:Key="BrText"    Color="{StaticResource ColText}"/>
        <SolidColorBrush x:Key="BrMuted"   Color="{StaticResource ColMuted}"/>
        <SolidColorBrush x:Key="BrRed"     Color="{StaticResource ColRed}"/>

        <!-- TextBlock default -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource BrText}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <!-- TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background"        Value="{StaticResource BrSurface}"/>
            <Setter Property="Foreground"        Value="{StaticResource BrText}"/>
            <Setter Property="BorderBrush"       Value="{StaticResource BrBorder}"/>
            <Setter Property="BorderThickness"   Value="1"/>
            <Setter Property="Padding"           Value="10,7"/>
            <Setter Property="FontFamily"        Value="Segoe UI"/>
            <Setter Property="FontSize"          Value="13"/>
            <Setter Property="CaretBrush"        Value="{StaticResource BrAccent}"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource BrAccent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- PasswordBox -->
        <Style TargetType="PasswordBox">
            <Setter Property="Background"      Value="{StaticResource BrSurface}"/>
            <Setter Property="Foreground"      Value="{StaticResource BrText}"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BrBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="10,7"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="13"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource BrAccent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Button base -->
        <Style TargetType="Button">
            <Setter Property="Background"      Value="{StaticResource BrSurface}"/>
            <Setter Property="Foreground"      Value="{StaticResource BrText}"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BrBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="16,8"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource BrAccent}"/>
                                <Setter Property="Foreground"  Value="{StaticResource BrAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary button (green) -->
        <Style x:Key="BtnPrimary" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background"  Value="{StaticResource BrAccent}"/>
            <Setter Property="Foreground"  Value="#000000"/>
            <Setter Property="FontWeight"  Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#44FF22"/>
                    <Setter Property="Foreground" Value="#000000"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary button (purple) -->
        <Style x:Key="BtnSecondary" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background"      Value="{StaticResource BrPurple}"/>
            <Setter Property="Foreground"      Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- ListBox -->
        <Style TargetType="ListBox">
            <Setter Property="Background"      Value="{StaticResource BrSurface}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground"      Value="{StaticResource BrText}"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding"     Value="14,10"/>
            <Setter Property="Foreground"  Value="{StaticResource BrText}"/>
            <Setter Property="Background"  Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrBg}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1A1A1A"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#1A1A1A"/>
                                <Setter Property="Foreground" Value="{StaticResource BrAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollBar slim dark -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="{StaticResource BrBg}"/>
            <Setter Property="Width"      Value="6"/>
        </Style>
    </Application.Resources>
</Application>
